def square(the_number):
    # initialize variable
    total = 0
    # repeat
    for counter in range(the_number):
        # change variable
        total = total + the_number
    return total

print(square(4))