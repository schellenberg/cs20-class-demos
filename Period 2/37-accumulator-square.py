def square(some_number):
    # initialize variable
    total = 0
    # repeat
    for counter in range(some_number):
        # update variable
        total = total + some_number
    return total

print(square(6))